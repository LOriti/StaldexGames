{
  "name": "lrmp",
  "version": "0.1.0",
  "private": true,
  "description": "LRMP — keto meal planner: paint a month by cooking mode, derive lunches from leftovers, bank surplus in the freezer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
